int Solution::coverPoints(vector<int> &A, vector<int> &B) {
    
    vector<pair<int,int>>v;
    int ans=0;
    
    for(i=1;i<n;i++)
    {
        if(A[i]==A[i-1])
        ans+=abs(A[i]-A[i-1])+
    }
}
