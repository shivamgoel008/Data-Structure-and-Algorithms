#include<iostream>
#include<algorithm>
#include<limits.h>
#include<math.h>
#include<limits>
#include<iomanip>
#define ll long long int 

using namespace std;

int main ()
{
    int n,p;
    cin>>n>>p;
    float z=sqrt(n);
    cout<<fixed<<setprecision(p)<<z; 
}